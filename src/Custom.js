import React, { Component } from "react";
import {
  Text,
  View,
  ScrollView,
  Dimensions,
  StatusBar,
  TouchableOpacity,
  Picker,
  Slider,
  Image
} from "react-native";

export default class Custom extends Component {
  constructor(props) {
    super(props);
  }
}
